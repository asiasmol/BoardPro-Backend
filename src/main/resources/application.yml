spring:
  datasource:
#    url: jdbc:postgresql://boardpro1.cd0uzudjuwyq.eu-north-1.rds.amazonaws.com:5432/BoardPro1
    url: jdbc:postgresql://localhost:5432/BoardPro
    username: asia
#    password: BoardPro123
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080